# Database
DATABASE_URL="postgresql://user:password@localhost:5432/mss_db?schema=public"

# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (для email подтверждения и ссылок в письмах)
FRONTEND_URL=http://localhost:3001

# ==========================================
# SMTP Configuration (для отправки email)
# ==========================================
# Если не указать SMTP настройки, будет использоваться тестовый SMTP (ethereal.email)
# В тестовом режиме письма не отправляются, но можно посмотреть их по ссылке в логах

# Для продакшена настройте один из вариантов:

# --- Вариант 1: Gmail ---
# Создайте App Password: https://myaccount.google.com/apppasswords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-character-app-password
# SMTP_FROM="MSS Студия <noreply@yourdomain.com>"

# --- Вариант 2: Yandex Mail ---
# SMTP_HOST=smtp.yandex.ru
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@yandex.ru
# SMTP_PASS=your-password
# SMTP_FROM="MSS Студия <noreply@yourdomain.com>"

# --- Вариант 3: Mail.ru ---
# ТРЕБУЕТСЯ пароль приложения! Создайте на: https://account.mail.ru/user/2-step-auth/passwords/
# 1. Включите двухфакторную аутентификацию
# 2. Создайте "Пароль для внешнего приложения" → "Почта"
# 3. Используйте сгенерированный пароль вместо обычного
# SMTP_HOST=smtp.mail.ru
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@mail.ru
# SMTP_PASS=your-app-password
# SMTP_FROM="MSS Студия <noreply@yourdomain.com>"

# --- Вариант 4: SendGrid ---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM="MSS Студия <noreply@yourdomain.com>"

# --- Вариант 5: Amazon SES ---
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password
# SMTP_FROM="MSS Студия <noreply@yourdomain.com>"

# ==========================================
# Tinkoff Acquiring
# ==========================================
# TINKOFF_TERMINAL_KEY=your-terminal-key
# TINKOFF_PASSWORD=your-password
# TINKOFF_NOTIFICATION_URL=https://your-domain.ru/api/orders/tinkoff/notification
# TINKOFF_SUCCESS_URL=https://your-domain.ru/profile?payment=success
# TINKOFF_FAIL_URL=https://your-domain.ru/profile?payment=fail
